{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Dev: Frontend & Backend",
			"type": "shell",
			"command": "npm install ts-node -g && npm install && npx prisma generate && npx prisma db push && npm run dev",
			"group": "build",
			"isBackground": true,
			"problemMatcher": [
				"$tsc"
			]
		}
	]
}